<div class="container">
  <div class="row">
    <h3>新規投稿フォーム</h3>
    <!--エラーメッセージ-->
    <%= render 'layouts/errors', obj: @post_image %>
  </div>
  <div class="row">
    <table class="table table-borderless">
      <tbody>
      <%= form_with model: @post_image, local: true do |f| %>
        <tr>
          <th scope="row">画像</th>
          <td><%= f.file_field :image, accept: "image/*" %></td>
        </tr>

        <tr>
          <th scope="row">投稿タイトル</th>
          <td><%= f.text_field :title %></td>
        </tr>

        <tr>
          <th scope="row">説明</th>
          <td><%= f.text_area :body %></td>
        </tr>

        <tr>
          <th scope="row">評価</th>
          <td>
            <div class="rating-box">
              <%= f.hidden_field :star, id:"rating-value" %>
              <div class="main-rating d-flex">
                <div class="ratings", style="cursor:pointer">
                  <span class="fa fa-star-o" id="star" style="color:#fc0"></span>
                  <span class="fa fa-star-o" id="star" style="color:#fc0"></span>
                  <span class="fa fa-star-o" id="star" style="color:#fc0"></span>
                  <span class="fa fa-star-o" id="star" style="color:#fc0"></span>
                  <span class="fa fa-star-o" id="star" style="color:#fc0"></span>
                </div>
                <div class="ml-3 rating-value-display">
                  <span id="rating-value-display">0</span>/5
                </div>
              </div>
            </div>
          </td>
        </tr>

        <tr>
          <th scope="row">タグ</th>
          <td><%= f.text_field :tag_name %></td>
        </tr>

        <tr>
          <th scope="row">店名</th>
          <td><%= f.text_field :shop_name %></td>
        </tr>

        <tr>
          <th scope="row">店の所在地</th>
          <td><%= f.text_field :shop_location %></td>
        </tr>

        <tr>
          <th scope="row">店のMAP</th>
          <td>
            <input id="address" type="textbox" value="店の住所を入力して検索">
            <input type="button" value="検索" onclick="codeAddress()">
            <p>マーカーをドラック＆ドロップで位置の調整ができます。<p>
            <div class="actions">
              <%= f.label :lat,"緯度" %>
              <%= f.text_field :lat,:value =>"緯度", id: :lat %>
              <%= f.label :lng,"経度" %>
              <%= f.text_field :lng,:value =>"経度", id: :lng %>
            </div>
            <div id='map' style="width: 600px; height: 600px;"></div>
            <div class="m-2"><%= f.submit "投稿する", class:"btn btn-secondary btn-lg" %></div>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>

  <%= link_to "一覧に戻る", post_images_path, class:"btn btn-info mt-2" %>

</div>




<script>
// 初期マップの設定
let map
let marker
function initMap(){
  geocoder = new google.maps.Geocoder()

  map = new google.maps.Map(document.getElementById('map'), {
    // 東京
    center:  {lat: 35.6803997, lng:139.7690174},
    zoom: 15,

  });
}

// 検索後のマップ作成
let geocoder
let aft
function codeAddress(){
  let inputAddress = document.getElementById('address').value;
  geocoder.geocode( { 'address': inputAddress}, function(results, status) {
    if (status == 'OK') {
        //マーカーが複数できないようにする
        if (aft == true){
            marker.setMap(null);
        }

        //新しくマーカーを作成する
        map.setCenter(results[0].geometry.location);
            marker = new google.maps.Marker({
            map: map,
            position: results[0].geometry.location,
            draggable: true	// ドラッグ可能にする
        });

        //二度目以降か判断
        aft = true

        //検索した時に緯度経度を入力する
        document.getElementById('lat').value = results[0].geometry.location.lat();
        document.getElementById('lng').value = results[0].geometry.location.lng();

        // マーカーのドロップ（ドラッグ終了）時のイベント
        google.maps.event.addListener( marker, 'dragend', function(ev){
            // イベントの引数evの、プロパティ.latLngが緯度経度
            document.getElementById('lat').value = ev.latLng.lat();
            document.getElementById('lng').value = ev.latLng.lng();
        });
    } else {
      alert('該当する結果がありませんでした：' + status);
    }
  });
}

</script>

<script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['API_KEY']%>&callback=initMap" async defer></script>

<%= javascript_pack_tag "star.js" %>
